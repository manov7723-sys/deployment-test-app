apiVersion: v1
kind: Service
metadata:
  name: deployment-test-app-worker
  namespace: storefront-dev
  labels:
    app.kubernetes.io/name: deployment-test-app-worker
    app.kubernetes.io/managed-by: deepagent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: deployment-test-app-worker
  ports:
    - name: http
      port: 80
      targetPort: 8080
