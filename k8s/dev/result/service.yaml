apiVersion: v1
kind: Service
metadata:
  name: deployment-test-app-result
  namespace: storefront-dev
  labels:
    app.kubernetes.io/name: deployment-test-app-result
    app.kubernetes.io/managed-by: deepagent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: deployment-test-app-result
  ports:
    - name: http
      port: 80
      targetPort: 80
