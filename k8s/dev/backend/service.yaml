apiVersion: v1
kind: Service
metadata:
  name: deployment-test-app-backend
  namespace: storefront-dev
  labels:
    app.kubernetes.io/name: deployment-test-app-backend
    app.kubernetes.io/managed-by: deepagent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: deployment-test-app-backend
  ports:
    - name: http
      port: 80
      targetPort: 3000
