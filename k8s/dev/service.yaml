apiVersion: v1
kind: Service
metadata:
  name: deployment-test-app
  namespace: storefront-dev
  labels:
    app.kubernetes.io/name: deployment-test-app
    app.kubernetes.io/managed-by: deepagent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: deployment-test-app
  ports:
    - name: http
      port: 80
      targetPort: 8080
